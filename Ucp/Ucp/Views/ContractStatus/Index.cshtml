@model IEnumerable<Ucp.Models.Data.ContractStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContrStatusVal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContrStatusVal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContractStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContractStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContractStatusId })
        </td>
    </tr>
}

</table>
